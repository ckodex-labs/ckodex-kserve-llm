---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: evalprofiles.serving.ckodex.com
spec:
  group: serving.ckodex.com
  names:
    kind: EvalProfile
    listKind: EvalProfileList
    plural: evalprofiles
    singular: evalprofile
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: EvalProfile is the Schema for the evalprofiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EvalProfileSpec defines the desired state of EvalProfile
            properties:
              mandatoryMetrics:
                additionalProperties:
                  type: number
                description: MandatoryMetrics defines the required score thresholds
                  for approval.
                type: object
              maxDurationSeconds:
                description: MaxDurationSeconds is the maximum time allowed for the
                  eval job.
                format: int32
                type: integer
              promptSet:
                description: PromptSet is a list of prompts used for benchmarking.
                items:
                  type: string
                type: array
              targetEngine:
                description: TargetEngine specifies the engine to run the eval on
                  (e.g., vllm, quant-cpp).
                type: string
            type: object
          status:
            description: EvalProfileStatus defines the observed state of EvalProfile
            properties:
              usageCount:
                description: UsageCount is the number of times this profile has been
                  used.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
