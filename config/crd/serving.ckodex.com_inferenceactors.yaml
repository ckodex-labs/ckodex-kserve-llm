---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: inferenceactors.serving.ckodex.com
spec:
  group: serving.ckodex.com
  names:
    kind: InferenceActor
    listKind: InferenceActorList
    plural: inferenceactors
    singular: inferenceactor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.actorType
      name: Type
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          InferenceActor is the stable v1 API for Dapr virtual actors
          that own model or agent state within the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceActorSpec defines the actor configuration.
            properties:
              actorType:
                description: ActorType classifies this actor.
                type: string
              agentRef:
                description: AgentRef binds to an Agent (for agent actors).
                type: string
              idleTimeout:
                default: 10m
                description: IdleTimeout deactivates the actor after inactivity.
                type: string
              maxConcurrency:
                default: 1
                description: MaxConcurrency limits concurrent requests per actor instance.
                format: int32
                type: integer
              modelRef:
                description: ModelRef binds to an LLMInferenceService (for model actors).
                type: string
              reentrancy:
                default: false
                description: Reentrancy allows the actor to process multiple requests
                  concurrently.
                type: boolean
              reminders:
                description: Reminders defines recurring timer-based callbacks.
                items:
                  description: ActorReminder defines a timer-based callback for an
                    actor.
                  properties:
                    data:
                      description: Data is opaque payload passed to the reminder callback.
                      type: string
                    dueTime:
                      description: DueTime is when the reminder first fires.
                      type: string
                    name:
                      description: Name of the reminder.
                      type: string
                    period:
                      description: Period is the recurrence interval (empty = one-shot).
                      type: string
                  required:
                  - dueTime
                  - name
                  type: object
                type: array
              stateStore:
                default: statestore
                description: StateStore is the Dapr state store component name for
                  actor state.
                type: string
            required:
            - actorType
            type: object
          status:
            description: InferenceActorStatus defines the observed actor state.
            properties:
              activeSessions:
                description: ActiveSessions tracks how many sessions are using this
                  actor.
                format: int32
                type: integer
              conditions:
                description: Conditions track actor health.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastActivationTime:
                description: LastActivationTime is when the actor was last activated.
                format: date-time
                type: string
              state:
                description: State is the actor lifecycle state.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.actorType
      name: Type
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          InferenceActor represents a Dapr virtual actor that owns model or agent state.
          Each LLMInferenceService or AgentSpec instance maps to one actor.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceActorSpec defines the actor configuration.
            properties:
              actorType:
                description: ActorType classifies this actor.
                type: string
              agentRef:
                description: AgentRef binds to an AgentSpec (for agent actors).
                type: string
              idleTimeout:
                default: 10m
                description: IdleTimeout deactivates the actor after inactivity.
                type: string
              maxConcurrency:
                default: 1
                description: MaxConcurrency limits concurrent requests per actor instance.
                format: int32
                type: integer
              modelRef:
                description: ModelRef binds to an LLMInferenceService (for model actors).
                type: string
              reentrancy:
                default: false
                description: Reentrancy allows the actor to process multiple requests
                  concurrently.
                type: boolean
              reminders:
                description: Reminders defines recurring timer-based callbacks.
                items:
                  description: ActorReminder defines a timer-based callback for an
                    actor.
                  properties:
                    data:
                      description: Data is opaque payload passed to the reminder callback.
                      type: string
                    dueTime:
                      description: DueTime is when the reminder first fires.
                      type: string
                    name:
                      description: Name of the reminder.
                      type: string
                    period:
                      description: Period is the recurrence interval (empty = one-shot).
                      type: string
                  required:
                  - dueTime
                  - name
                  type: object
                type: array
              stateStore:
                default: statestore
                description: StateStore is the Dapr state store component for actor
                  state.
                type: string
            required:
            - actorType
            type: object
          status:
            description: InferenceActorStatus defines the observed actor state.
            properties:
              activeSessions:
                description: ActiveSessions tracks how many sessions are using this
                  actor.
                format: int32
                type: integer
              conditions:
                description: Conditions track actor health.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastActivationTime:
                description: LastActivationTime is when the actor was last activated.
                format: date-time
                type: string
              state:
                description: State is the actor lifecycle state.
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
