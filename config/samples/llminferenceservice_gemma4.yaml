apiVersion: serving.ckodex.com/v1alpha2
kind: LLMInferenceService
metadata:
  name: gemma-4-2b
  namespace: ckodex-inference
spec:
  model:
    uri: hf://google/gemma-4-E2B-it
    name: google/gemma-4-E2B-it
  replicas: 1
  # Notice: we don't specify any container details or resource limits here.
  # The operator will automatically apply the WellKnown defaults for this model.
  router:
    gateway:
      managed:
        gatewayClassName: envoy
    route:
      httpRoute: {}
    scheduler:
      pool: {}
