apiVersion: serving.ckodex.com/v1alpha2
kind: LLMInferenceService
metadata:
  name: gemma-4-e4b
  namespace: ckodex-inference
spec:
  model:
    uri: hf://google/gemma-4-E4B-it
    name: google/gemma-4-E4B-it
  replicas: 1
  # The operator will automatically apply the WellKnown defaults for this model (8 CPU, 32Gi RAM, 1 GPU).
  router:
    gateway:
      managed:
        gatewayClassName: envoy
    route:
      httpRoute: {}
    scheduler:
      pool: {}
