// Package version holds build-time version metadata for release artifacts.
package version

// Version is the release version injected at build time.
// Defaults to dev for local builds.
var Version = "dev"
